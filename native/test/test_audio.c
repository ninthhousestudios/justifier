// test_audio.c — stub for CMake configuration
// Full implementation: Plan 02
// C test harness — no Flutter/Dart dependency

#include "justifier_audio.h"
#include <stdio.h>

// TODO: Plan 02 implements the full test harness
// - justifier_init(48000, 256)
// - justifier_voice_add(WAVEFORM_SINE, 440.0f, 0.3f)
// - sleep(3) and listen
// - justifier_get_xrun_count()
// - justifier_shutdown()

int main(void) {
    printf("test_audio stub — full implementation in Plan 02\n");
    return 0;
}
